[DEFAULT]

dist = DEP14
debian-branch = debian/latest
upstream-branch = upstream/latest
pristine-tar = True
pristine-tar-commit = True
sign-tags = True

[create-remote-repo]
remote-url-pattern = git@salsa.debian.org:debian/%(pkg)s.git
